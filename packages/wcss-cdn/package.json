{
  "name": "wcss-cdn",
  "version": "0.1.0",
  "description": "CDN bundle for WCSS framework - use WCSS without a build step",
  "main": "dist/wcss-cdn.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "tsc && npm run bundle",
    "bundle": "esbuild dist/index.js --bundle --minify --outfile=dist/wcss-cdn.js --format=iife --global-name=WCSS",
    "dev": "tsc --watch"
  },
  "keywords": [
    "wcss",
    "css",
    "cdn",
    "browser",
    "runtime"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.10.0",
    "esbuild": "^0.19.0",
    "typescript": "^5.3.2"
  }
}
